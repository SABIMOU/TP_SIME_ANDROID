// Code generated by moshi-kotlin-codegen. Do not edit.
@file:Suppress("DEPRECATION", "unused", "UNUSED_PARAMETER", "ClassName", "REDUNDANT_PROJECTION",
    "RedundantExplicitType", "LocalVariableName", "RedundantVisibilityModifier",
    "PLATFORM_CLASS_MAPPED_TO_KOTLIN", "IMPLICIT_NOTHING_TYPE_ARGUMENT_IN_RETURN_POSITION")

package fr.mastersid.etudiant.template.`data`.remote.dto

import com.squareup.moshi.JsonAdapter
import com.squareup.moshi.JsonReader
import com.squareup.moshi.JsonWriter
import com.squareup.moshi.Moshi
import com.squareup.moshi.`internal`.Util
import java.lang.NullPointerException
import java.lang.reflect.Constructor
import kotlin.Int
import kotlin.Long
import kotlin.String
import kotlin.Suppress
import kotlin.collections.emptySet
import kotlin.jvm.Volatile
import kotlin.text.buildString

public class QuestionDtoJsonAdapter(
  moshi: Moshi,
) : JsonAdapter<QuestionDto>() {
  private val options: JsonReader.Options = JsonReader.Options.of("question_id", "title",
      "answer_count", "last_activity_date", "body")

  private val intAdapter: JsonAdapter<Int> = moshi.adapter(Int::class.java, emptySet(),
      "questionId")

  private val stringAdapter: JsonAdapter<String> = moshi.adapter(String::class.java, emptySet(),
      "title")

  private val longAdapter: JsonAdapter<Long> = moshi.adapter(Long::class.java, emptySet(),
      "lastActivityDate")

  private val nullableStringAdapter: JsonAdapter<String?> = moshi.adapter(String::class.java,
      emptySet(), "body")

  @Volatile
  private var constructorRef: Constructor<QuestionDto>? = null

  override fun toString(): String = buildString(33) {
      append("GeneratedJsonAdapter(").append("QuestionDto").append(')') }

  override fun fromJson(reader: JsonReader): QuestionDto {
    var questionId: Int? = null
    var title: String? = null
    var answerCount: Int? = null
    var lastActivityDate: Long? = 0L
    var body: String? = null
    var mask0 = -1
    reader.beginObject()
    while (reader.hasNext()) {
      when (reader.selectName(options)) {
        0 -> questionId = intAdapter.fromJson(reader) ?: throw Util.unexpectedNull("questionId",
            "question_id", reader)
        1 -> title = stringAdapter.fromJson(reader) ?: throw Util.unexpectedNull("title", "title",
            reader)
        2 -> answerCount = intAdapter.fromJson(reader) ?: throw Util.unexpectedNull("answerCount",
            "answer_count", reader)
        3 -> {
          lastActivityDate = longAdapter.fromJson(reader) ?:
              throw Util.unexpectedNull("lastActivityDate", "last_activity_date", reader)
          // $mask = $mask and (1 shl 3).inv()
          mask0 = mask0 and 0xfffffff7.toInt()
        }
        4 -> {
          body = nullableStringAdapter.fromJson(reader)
          // $mask = $mask and (1 shl 4).inv()
          mask0 = mask0 and 0xffffffef.toInt()
        }
        -1 -> {
          // Unknown name, skip it.
          reader.skipName()
          reader.skipValue()
        }
      }
    }
    reader.endObject()
    if (mask0 == 0xffffffe7.toInt()) {
      // All parameters with defaults are set, invoke the constructor directly
      return  QuestionDto(
          questionId = questionId ?: throw Util.missingProperty("questionId", "question_id",
              reader),
          title = title ?: throw Util.missingProperty("title", "title", reader),
          answerCount = answerCount ?: throw Util.missingProperty("answerCount", "answer_count",
              reader),
          lastActivityDate = lastActivityDate as Long,
          body = body
      )
    } else {
      // Reflectively invoke the synthetic defaults constructor
      @Suppress("UNCHECKED_CAST")
      val localConstructor: Constructor<QuestionDto> = this.constructorRef ?:
          QuestionDto::class.java.getDeclaredConstructor(Int::class.javaPrimitiveType,
          String::class.java, Int::class.javaPrimitiveType, Long::class.javaPrimitiveType,
          String::class.java, Int::class.javaPrimitiveType, Util.DEFAULT_CONSTRUCTOR_MARKER).also {
          this.constructorRef = it }
      return localConstructor.newInstance(
          questionId ?: throw Util.missingProperty("questionId", "question_id", reader),
          title ?: throw Util.missingProperty("title", "title", reader),
          answerCount ?: throw Util.missingProperty("answerCount", "answer_count", reader),
          lastActivityDate,
          body,
          mask0,
          /* DefaultConstructorMarker */ null
      )
    }
  }

  override fun toJson(writer: JsonWriter, value_: QuestionDto?) {
    if (value_ == null) {
      throw NullPointerException("value_ was null! Wrap in .nullSafe() to write nullable values.")
    }
    writer.beginObject()
    writer.name("question_id")
    intAdapter.toJson(writer, value_.questionId)
    writer.name("title")
    stringAdapter.toJson(writer, value_.title)
    writer.name("answer_count")
    intAdapter.toJson(writer, value_.answerCount)
    writer.name("last_activity_date")
    longAdapter.toJson(writer, value_.lastActivityDate)
    writer.name("body")
    nullableStringAdapter.toJson(writer, value_.body)
    writer.endObject()
  }
}
